"use client";

import FinalCta from "@/components/FinalCta";

/** FinalCta eager — nav / hero #contact hash scroll’u lazy fold’a takılmasın */
export default function HomeTailFold() {
  return <FinalCta />;
}
